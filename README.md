CMAKE_BASH_WINDOWS_TEST

Instructions:
Clone somewhere in the Linux subsystem. Does not matter where.
Just go in to the build folder and execute: `cmake Unix Makefiles .. `
The CMake will fail and will not produce a makefile
